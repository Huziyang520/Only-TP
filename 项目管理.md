# OnlyTP 项目管理

> 一个 Minecraft 1.20.1 模组：限制玩家只能使用 TP 相关命令，或完全禁用非 TP 命令。

---

## 1. 项目概览

| 项目 | 值 |
|---|---|
| Mod ID | `onlytp` |
| 当前版本 | `1.3.0` |
| 游戏版本 | Minecraft `1.20.1` |
| 支持平台 | Forge `47.2.30` / Fabric Loader `0.16.9` |
| Java | 17 |
| License | MIT |
| 作者 | Huziyang520 |
| 前置依赖 | **AvalonBase**（必需，作为前置 mod） |

### 项目职责
- 限制玩家只能执行 TP 相关命令，或完全禁用非 TP 命令
- 限制玩家切换游戏模式的权限
- 提供可配置的 GUI 设置界面（基于 AvalonBase 主题系统）
- 配置在客户端与服务端之间同步

---

## 2. 模块结构（MultiLoader）

```
Only TP/1.20.1/
├─ common/     # 共享逻辑：命令逻辑、游戏模式逻辑、配置、GUI、网络包
├─ fabric/     # Fabric 平台（入口、GUI 事件、mixin、客户端）
├─ forge/      # Forge 平台（入口、GUI 事件）
├─ buildSrc/   # 自定义 Gradle 插件
├─ build.gradle / settings.gradle / gradle.properties
└─ gradlew[.bat] / gradle/
```

> ⚠️ **README.md 仍是 MultiLoader 模板的英文默认内容**，尚未定制为 OnlyTP 的介绍文档，建议后续补充。

---

## 3. 源码文件关系

### common 模块（`com.onlytp.onlytpmod`，11 个文件）
| 包 | 文件 | 职责 |
|---|---|---|
| 根 | `CommonClass` | 网络消息注册入口（被平台入口调用） |
| 根 | `Constants` | mod_id / 名称 / Logger |
| `config` | `OnlyTPConfig` | 命令/游戏模式限制的配置项 |
| `event` | `CommandLogic` | 命令过滤/拦截核心逻辑 |
| `event` | `CommandRegistrationLogic` | 命令注册相关逻辑 |
| `event` | `GameModeLogic` | 游戏模式切换限制逻辑 |
| `event` | `PlayerJoinLogic` | 玩家加入时的处理 |
| `gui` | `OnlyTPScreen` | 设置界面（继承 AvalonBase 配置界面） |
| `network` | `ConfigSyncPacket` | 服务端→客户端配置同步 |
| `network` | `ConfigUpdatePacket` | 客户端→服务端配置更新 |
| `network` | `NetworkChannels` | 网络通道 ID 定义 |

### fabric 模块（6 个文件）
| 文件 | 职责 |
|---|---|
| `OnlyTPMod` | Fabric 模组入口 |
| `client/OnlyTPClient` | Fabric 客户端初始化 |
| `event/GuiEventHandler` | 暂停界面注入设置按钮 |
| `mixin/CommandsMixin` | 命令注册钩子 |
| `mixin/ScreenAccessor` | 屏幕控件注入访问器 |
| `mixin/ServerPlayerMixin` | 服务端玩家逻辑钩子 |

### forge 模块（2 个文件）
| 文件 | 职责 |
|---|---|
| `OnlyTPMod` | Forge 模组入口 |
| `event/GuiEventHandler` | 暂停界面注入设置按钮 |

### 依赖关系
- `OnlyTP` **依赖** `AvalonBase`（编译期引用其 jar，运行期作为前置 mod）。
- `fabric`/`forge` 模块 → `common` 模块。
- 与 `AuthCmd` 相互独立（同为 AvalonBase 下游，互不依赖）。

---

## 4. 版本与兼容性

| 项 | 版本 | 备注 |
|---|---|---|
| 模组版本 | `1.3.0` | |
| Minecraft 范围 | `[1.20.1, 1.20.5)` | |
| Parchment | `2023.09.03` | |
| Fabric API | `0.92.1+1.20.1` | |
| AvalonBase 依赖 | `1.1.0` | 需与本机 AvalonBase 版本一致 |

> ⚠️ **依赖同步提醒**：`common/fabric/forge` 三个 `build.gradle` 中对 AvalonBase jar 的文件名写死了版本号。AvalonBase 升级后必须同步修改（当前已统一为 `1.1.0`）。

---

## 5. 进度追踪（Tasks）

| 状态 | 任务 | 说明 |
|---|---|---|
| ✅ 完成 | TP 命令限制 / 非 TP 命令禁用 | 核心功能 |
| ✅ 完成 | 游戏模式切换限制 | |
| ✅ 完成 | GUI 设置界面 | 基于 AvalonBase 主题系统 |
| ✅ 完成 | 配置客户端↔服务端同步 | 基于 AvalonBase 网络抽象 |
| ✅ 完成 | 暂停界面注入设置按钮 | 点击打开 `OnlyTPScreen` |
| ✅ 完成 | 移除暂停界面按钮「悬停手形」 | 见决策记录 |
| ✅ 完成 | 版本 `1.3.0` 构建 | Forge/Fabric 双端 |
| 🔲 待定 | 定制 README 文档 | 当前仍为模板英文内容 |
| 🔲 待定 | 发布 `1.3.0` | 需捆绑 AvalonBase 前置 |

---

## 6. 决策记录（Decision Log）

| 日期 | 决策 | 理由 / 结果 |
|---|---|---|
| - | 采用 MultiLoader 三模块结构 | 一套 common 源码同时支持 Forge/Fabric |
| - | 网络消息经 `AvalonNetwork` 平台无关抽象 | 复用 AvalonBase 网络层，统一 API |
| 2026-08-16 | **删除**暂停界面（PauseScreen）按钮悬停变手形的功能 | 手形触发后无法恢复箭头，用户体验异常；彻底放弃该行为 |
| 2026-08-16 | **保留**编辑界面（`OnlyTPScreen`）按钮的原版手形 | 编辑界面手形走原版 `Screen` 机制，与 `GuiCursor` 无关，不受影响 |
| 2026-08-16 | `GuiCursor.applyHand()` 从 `GuiEventHandler` 移除 | 暂停界面不再强制改光标，从根源消除「手形变不回去」问题 |
| 2026-08-16 | AvalonBase 依赖从 `1.0.0` 更新为 `1.1.0` | 修复 forge 依赖 jar 文件不存在导致的编译失败 |

---

## 7. 构建说明

```bash
# 在 Only TP/1.20.1 目录下
./gradlew build          # 全量构建
./gradlew :fabric:build  # 仅 fabric
./gradlew :forge:build   # 仅 forge
```

> 前置条件：先构建 AvalonBase，确保其 `fabric/build/libs`、`forge/build/libs`、`fabric/build/devlibs` 下有对应版本 jar。

> 构建产物：`fabric/build/libs/onlytp-fabric-1.20.1-1.3.0.jar`、`forge/build/libs/onlytp-forge-1.20.1-1.3.0.jar`。
